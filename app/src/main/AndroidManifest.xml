<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.groovemood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrooveMood"
        android:name=".GrooveMood">
        <activity
            android:name=".MusicScreen"
            android:exported="false"
            android:theme="@style/Theme.SlideFromBottom"/>
        <activity
            android:name=".ViewPlaylistScreen"
            android:exported="false" />
        <activity
            android:name=".GeneratePlaylist"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>